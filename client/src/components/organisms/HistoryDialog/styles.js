const styles = theme => ({
  dialog: {
    padding: 20,
  },
  done: {
    padding: '10px 0',
    color: theme.palette.primary.main,
  },
  unDone: {
    padding: '10px 0',
    color: theme.palette.secondary.light,
  },
})

export default styles
